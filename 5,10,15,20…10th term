#include <stdio.h>

void printSeries(int n);

int main() {
    int n = 10;   // number of terms
    printSeries(n);
    return 0;
}

void printSeries(int n) {
	int term=5;
    printf("Series up to %d terms: ", n);
    for (int i = 1; i <= n; i++) {
        printf("%d ", term); 
		  term=term+5;
    }
    printf("\n");
}
/* Series up to 10 terms: 5 10 15 20 25 30 35 40 45 50 */
